# Secrets — never commit
.env

# Local state written by the pollers (state.json, state-jobhive.json, state-nuworks.json, ...)
state.json
state-*.json

# Cached jobhive companies dataset (re-downloaded on demand)
.cache/

# Python
__pycache__/
*.pyc
.venv/

# Experimentation scratch (slug CSVs, discover DBs, keep lists, tranco cache)
tmp

# Handshake session cookie jar (live auth — never commit)
engine/handshake_cookies.json
engine/handshake_profile/

engine/wellfound_profile/

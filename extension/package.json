{
  "name": "job-collector-extension",
  "description": "Background MV3 extension that collects lightweight job listings from logged-in boards.",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "@types/chrome": ">=0.0.260",
    "typescript": "^5.5.4",
    "wxt": "^0.19.0"
  }
}
